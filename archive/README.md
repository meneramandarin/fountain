# Local Archive

This directory is for local-only archived database artifacts.

The application runtime uses Neon Postgres only. SQLite files such as `canonical.db`,
per-source scrape databases, and local database backups are ignored here so they do
not become part of the active repository workflow.
